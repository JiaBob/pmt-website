{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "predeploy": "yarn install && yarn generate",
    "deploy": "cd .output/public && echo >> .nojekyll && echo privacypromedical.com > CNAME && git init && git add -A && git commit -m 'deploy' && git push -f git@github.com:JiaBob/pmt-website.git master:gh-pages && cd ../..",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@heroicons/vue": "^2.0.13",
    "@nuxtjs/i18n": "^8.0.0-beta.6",
    "@nuxtjs/i18n-edge": "8.0.0-beta.7-27851181.caaea70",
    "@nuxtjs/tailwindcss": "^6.1.3",
    "@vueuse/components": "^9.6.0",
    "@vueuse/core": "^9.6.0",
    "@vueuse/nuxt": "^9.6.0",
    "nuxt": "^3.0.0",
    "swiper": "^8.4.5",
    "three": "^0.147.0",
    "video.js": "^7.20.3"
  }
}
